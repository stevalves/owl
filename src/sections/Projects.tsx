interface iProjects {

}

const Projects = ({}: iProjects) => {
  return (
    <section id="projects" className="w-full h-screen bg-coffee-700">
      <h1>Projects</h1>
    </section>
  );
};

export default Projects;